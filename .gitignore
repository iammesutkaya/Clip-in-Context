# Secrets & local state (never commit)
config.json
client_secret.json
youtube_token.json
jargon_cache.json

# Python
__pycache__/
*.pyc
venv/
.venv/

# macOS
.DS_Store

# Double-clickable launcher (regenerate with osacompile — see README)
Clip Backtrack.app/
