# Secrets & local state (never commit)
config.json
client_secret.json
youtube_token.json
jargon_cache.json

# Python
__pycache__/
*.pyc
venv/
.venv/

# macOS
.DS_Store

# Double-clickable launcher (regenerate with osacompile — see README)
*.app/

# Local clip history
clips.jsonl

# Local design/scratch archive (not published)
archive/

# Local screenshots — ignore the folder, except shots referenced by the README
screenshots/*
!screenshots/260730_clip-in-context.png
